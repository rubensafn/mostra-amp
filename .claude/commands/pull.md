Atualiza as duas branches (dev e main) com as últimas mudanças do repositório remoto.

Execute em ordem:

1. `git checkout main`
2. `git pull origin main`
3. `git checkout dev`
4. `git pull origin dev`
5. Informe quantos commits foram puxados em cada branch (ou que já estavam atualizadas)
